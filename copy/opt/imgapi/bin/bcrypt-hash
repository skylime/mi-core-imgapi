#!/usr/bin/env python

import sys
import bcrypt
import warnings

warnings.filterwarnings('ignore')

if len(sys.argv) > 1:
    print(bcrypt.hashpw(str(sys.argv[1]), bcrypt.gensalt()))
else:
    print('Missing password argument')
    sys.exit(1)
